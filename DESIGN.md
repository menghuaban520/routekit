# RouteKit 网络与分流工具

## 当前方向 · v0.5

用户否决了 v0.3 过黑、小字、厚重按钮和缺少操作引导的方向。本版优先可读性与任务连贯性：网络检查、订阅与节点、分流配置、规则诊断共用导航；导入的节点能直接用于默认代理与应用分流，预览显示实际绑定，下载后知道如何在客户端导入和验证。

- 颜色：画布 #f4f7fb，面板 #ffffff，正文 #172b46，次要文字 #64748b，边线 #dce4ef，操作 #246bfd，选中底色 #edf3ff。成功、警告与错误采用各自语义色。
- 字体：系统 sans-serif，正文 14–15px，辅助标签至少 12px，页面标题 25–30px，面板标题 18–20px。IP、采样及代码使用等宽字体。手机不通过缩小字号挤入桌面布局。
- 结构：桌面 208px 侧栏，1100px 以下转换为顶部完整导航；配置页左编辑右预览，900px 以下上下排列。4/8/12/16/24/32 间距，6–10px 圆角、细边线；按钮使用实色和明确选中边框，不做厚重投影或光泽。
- 教学在操作处出现：订阅导入三步、分流四步、默认节点选择、应用去向预览、自定义域名示例。较长背景解释可折叠，错误与恢复操作保持可见。各区段可直接跳转。
- 网络页用当前连接、主机查询、泄漏检查三个页签组织内容；保留真实流式采样，未测值未知。DoH 查询只显示实际记录，不把 HTTPS 耗时称为 ICMP Ping，也不将外部测试入口标成“已通过”。
- 实时上下行来自本地 Mihomo 只读监测器；总速度和活跃连接增量分别说明来源。Shadowrocket 用户使用客户端数据页。停止、离页、隐藏窗口及请求失败会暂停监测；重连重新建立采样基线。
- 数据边界：订阅 URL 与监测令牌只留当前会话；显式保存的配置可包含绑定节点凭证，保存/导出附近说明。参考节点的原 URI 参数不丢弃，使用配套节点文件并教用户先节点后配置。
- 保持键盘焦点、Escape、取消与减少动态效果；真实数据更新与表现动画分开。

## 功能分类与连续流程

- 网络检查：网络概览、速度测试、主机查询、泄漏检查；概览的一次检测只查出口、HTTPS 延迟和连通性，下载测速独立开始。
- 订阅与节点：导入订阅、套餐用量、节点列表、批量实测、实时流量。一次只显示当前工具，节点、用量和检测状态在分类间共享。
- 分流配置：应用分流、连接设置、DNS 设置、链式代理、批量规则检查；高级编辑继续在配置工作台内使用。
- 桌面侧栏分组，短窗口可滚动；手机显示三组入口与当前组的小分类，不把全部工具塞进一排。每项有独立 view/tool 地址，浏览器前进后退能恢复位置；切换功能不清空未保存的编辑。
- 节点检测由本地助手操作隔离内核；网页显示逐节点进度和结果，可取消。连接助手后可选择导入完成就检测；5 MB 下载测速另行选择，显示预计检测流量。结果可直接用于节点排序和应用分流。套餐额度来自服务商快照，不使用检测流量反推账单。
- 延用 v0.4 蓝白颜色和字号。开关与复选框不继承普通输入框的最小高度，保持开/关形状与可点击标签。

## 视觉参考与核验

[v0.4 基础视觉概念](docs/design-concept-v0.4.png)由内置 Image Gen 生成，用于完整配置工作台的色彩、字体、导航、双栏、应用行和引导节奏。简要生成要求为“中文代理分流工具，浅蓝灰画布、白色面板、清晰字体、平面蓝色按钮、侧栏、步骤引导、应用节点选择与右侧路由预览”。生产界面使用 React/CSS/Lucide，不加载概念图片作为功能界面。

概念中自动生成的账号入口、Clash 标签、通配符教程与虚构地区示例不属于产品能力：实际界面保留无账号、本地处理，使用 Shadowrocket 格式、合法域名示例及用户导入的节点。这些是为正确性作出的明确修正。基础设置保留在原键盘顺序，默认显示应用分流；节点依赖及凭证说明按真实导出模式显示。

验收比较五项：白色面板/浅蓝灰画布、正常字号与字重、左栏及手机顶部导航、平面按钮/选中态、应用与节点到预览及导出的位置关系。新增默认节点控件与配套节点教程会增加工作台高度，属于打通实际流程的必要内容。实际下载操作放到源码预览之前，使首次使用者能在查看应用去向后直接导出。截图与交互证据见 [验证记录](docs/verification.md)。

网络检测的功能拆分参考 [LibreSpeed 官方示例](https://github.com/librespeed/speedtest/blob/master/examples/example-singleServer-pretty.html)的开始/停止和指标分区，以及 [Cloudflare speedtest](https://github.com/cloudflare/speedtest)的分项测量。另读了 [LibreSpeed 用户对 Mbps/MBps 的疑问](https://github.com/librespeed/speedtest-android/issues/38)，速度页同时提供 Mbps、MB/s 与换算解释；这是通用单位可理解性的参考，不将该历史反馈当作当前产品性能结论。
