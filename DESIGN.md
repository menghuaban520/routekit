# RouteKit design

目标：让不熟悉路由语法的人，通过选择应用与连接方式，生成可检查、可备份的 Shadowrocket 分流配置。新手与高级模式编辑同一份方案。后续客户端由适配器接入。

采用本次生成的工作台概念：顶部品牌与帮助入口；标题后进入客户端和模式选择；左侧分组表单，右侧始终可见的配置预览与下载。应用是有分隔线的行，不堆叠卡片。DNS、链式说明、保存方案遵循同一信息层级。

- 颜色：页面 #f5f7fa；面板 #fff；正文 #1c2637；次要文字 #596779；操作 #147d73；选中 #e9f5f2；警告 #8b5513。正文对比度至少 4.5:1。
- 字体：系统 sans-serif，中文 PingFang SC / Microsoft YaHei；代码 ui-monospace。标题 28–40px，面板标题 19px，正文 14–15px，辅助文字 12–13px。
- 间距：4/8/12/16/24/32/48。主体 1240px；双栏 1.6:1，24px 间隔。细边框、无装饰阴影；面板圆角 12px，输入 7px。
- 图标：lucide-react 18–20px；应用使用文字缩写的色块，以免未经授权复用品牌图片。
- 状态：选择通过底色、描边和文字共同表达；可见焦点；表单错误就地提示，导出错误阻止下载。保存成功仅在存储成功后显示。
- 响应式：960px 以下单栏；600px 以下缩紧留白，应用动作换行。所有正文和输入可缩放；代码区单独滚动。
- 动效仅用于控件反馈，120ms；prefers-reduced-motion 下取消。模态框支持 Escape、焦点约束、关闭后恢复焦点。
- 数据保存在用户明确操作之后；不上传配置、不请求远端规则、不读取用户节点。

## v0.2 工具箱扩展

沿用已选定的白色工作台、蓝灰背景与青绿色操作。一级导航为网络概览、订阅与节点、分流配置、批量检查；原双栏配置器保留在分流配置中，切换不丢失编辑状态。概览以实际测量数值和操作为主，订阅数据与本地检测来源分开标注。窄屏导航可横向滚动，表格在自身容器内滚动；未知和未测量结果显示明确占位，不使用示意指标伪装实测。
