# RouteKit 网络观测台

## 当前方向 · v0.3

使用场景是用户在桌面集中试节点、盯住实时数值、比较出口与线路。界面以深墨色观测台突出数据，收起解释性的常驻段落；数字、采样曲线和实际事件形成反馈，不用模拟数值营造繁忙感。

- 四个工作区：网络观测、订阅与节点、分流配置、批量检查。一级导航保持一致，切换不丢失当前编辑。
- 颜色：底色 #0b1215，面板 #111b1e，浮层 #182529，正文 #e3efed，次要文字 #9aabad，边线 #2d3e42，强调 #67e8bd。绿色用于操作、选中和真实完成状态；琥珀色用于待定状态，红色用于错误。
- 字体：界面使用系统 sans-serif；IP、速度、时间和代码使用系统 monospace 与等宽数字。工作区标题 24px，面板标题 16–18px；数据可放大至 64px，避免把说明做成大标题。
- 信息优先级：网络页先 IP、吞吐、采样和事件；订阅页先额度与节点，添加入口独立分区；配置保留左编辑、右预览。大段解释进入可展开说明，输入标签、5 MB 用量、覆盖行为、错误和来源时间保留。
- 动效由真实活动驱动：下载字节与采样曲线随流式读取更新，检测事件逐项出现，停止立即取消请求；不自动测速或持续后台轮询。尊重 prefers-reduced-motion，取消表现动画而保留数据更新。
- 间距：4/8/12/16/24/32。主体 1240px，面板细描边与 10px 圆角，采用紧凑行结构。手机保留完整入口，数据与表格在自身容器中适配，不让页面横向溢出。
- 焦点、错误恢复、弹窗 Escape 与焦点返回保持有效；数据复制和导出须与真实结果一致。
- 当前值、历史快照、未知和失败清晰区分；不得随机补点、猜 IP 类型、或把当前浏览器结果视为所有代理节点的结果。

## 延续范围

初版选择白色双栏分流工作台；v0.2 扩展了订阅、测量与诊断能力。v0.3 根据用户反馈调整视觉密度与测量反馈，配置模型、客户端语法、订阅隐私范围和本地检测器流程保持原设计约束。
