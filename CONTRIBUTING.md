# 贡献指南

欢迎修复问题、完善应用域名以及增加客户端适配。提交较大的新功能前，先通过 Issue 说明实际使用场景与计划支持的客户端版本。

## 本地检查

使用 Node.js 24，安装锁定的依赖并启动开发服务：

```sh
npm ci
npm run dev
```

提交前运行：

```sh
npm run check
npx playwright install chromium
npm run test:e2e
```

配置引擎变更应覆盖规则优先级、非法输入与实际导出结果；界面变更需检查桌面、窄屏和键盘操作。修改生产响应头时，还应通过 `npx wrangler dev` 检查构建产物；Vite 开发服务器不会自动应用 `public/_headers`。

## 配置与应用目录

- 域名条目应提供可核对的来源和日期，并说明是否使用共享 CDN。不要因为出现品牌名称就把整个公共后缀设为直连。
- 默认数据应使用可公开的示例。不要提交真实节点地址、密码、订阅、个人配置或浏览器保存文件。
- 新客户端通过独立导出器适配，明确不支持的字段，不应静默丢失规则或仅替换扩展名。
- 在客户端中实际导入后，记录客户端版本和测试结果。生成语法、成功导入、路由生效以及无 DNS 泄漏是不同的验证层次。
- 导出内容应为可预期的配置文本；禁止通过换行、分隔符或自定义规则注入其他配置段。

## 保持浏览器内处理

项目无需服务端处理用户配置。增加联网、统计、节点凭证或云同步会改变隐私范围，请先说明必要性并同步修改隐私说明。不要为修复 CSP 报错直接放开任意脚本或外部连接。

Pull Request 请说明解决的问题、实际行为和已经执行的检查。引用其他开源项目的代码或规则前先检查其许可证，按要求保留署名和声明。你提交的原创贡献按仓库的 MIT 许可发布。
